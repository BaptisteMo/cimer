import { LoginForm } from '@/components/auth/login-form';

export const metadata = {
  title: 'Login | CMR Digital',
  description: 'Sign in to your CMR Digital account',
};

export default function LoginPage() {
  return <LoginForm />;
}
