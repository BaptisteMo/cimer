import { SignupForm } from '@/components/auth/signup-form';

export const metadata = {
  title: 'Sign Up | CMR Digital',
  description: 'Create your CMR Digital account',
};

export default function SignupPage() {
  return <SignupForm />;
}
